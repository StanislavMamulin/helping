import { StyleSheet } from 'react-native';
import { textStyles } from '../../commonStyles/text';

const styles = StyleSheet.create({
  container: {
    marginVertical: 8,
  },
  title: {
    ...textStyles.TEXT_STYLE_NKO_TITLE,
  },
});

export default styles;
