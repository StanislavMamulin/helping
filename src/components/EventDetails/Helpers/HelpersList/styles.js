import { StyleSheet } from 'react-native';

export const AVATAR_SIZE = 36;

export const styles = StyleSheet.create({
  avatarsContainer: {
    flexDirection: 'row',
    marginRight: -10,
  },
  avatarBorder: {
    borderWidth: 2,
  },
});
