import { StyleSheet } from 'react-native';
import { sizes } from '../../commonStyles/sizes';

export const styles = StyleSheet.create({
  contentContainer: {
    padding: 8,
    paddingBottom: sizes.CENTRAL_BUTTON_OFFSET,
  },
});
