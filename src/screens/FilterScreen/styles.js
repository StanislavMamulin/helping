import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  filterIcon: {
    width: 24,
    height: 24,
    tintColor: 'white',
    marginRight: 20,
  },
});

export default styles;
